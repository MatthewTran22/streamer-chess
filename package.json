{
  "name": "mentraos-tts-app",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "start": "bun src/index.ts",
    "dev": "bun --hot src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "description": "MentraOS TTS app that calls backend API and speaks the response when button is pressed",
  "dependencies": {
    "@mentra/sdk": "^2.1.21"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^22.13.14"
  }
}
